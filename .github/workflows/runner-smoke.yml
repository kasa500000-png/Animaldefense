name: Runner Smoke Test

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/runner-smoke.yml'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub-hosted runner works
        run: |
          echo "runner=ok"
          uname -a
